# Optional override for both development and production builds.
# Copy to .env.local only when the backend address changes.
VITE_API_BASE_URL=https://mitinvladimir416-glitch-botyara-api-e748.twc1.net
